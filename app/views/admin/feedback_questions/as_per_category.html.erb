<h3><%= link_to "New feedback question", new_admin_feedback_question_path(product_category_id: params[:product_category_id]) %> || <%= link_to "Back", admin_product_categories_path%></h3>

<%# <h2>Feedback questions</h2>
<select name="Answer type">
  <option value="">Answer type</option>
  <option value="single choice">Single Choice</option>
  <option value="multiple choice">Multiple Choice</option>
  <option value="text">Text</option>
</select> %>

<% @feedback_question.each do |feedback_question| %>
  <ul><li><%= feedback_question.question %>
  <%= button_to 'Destroy', { controller: :feedback_questions, action: :destroy, id: feedback_question.id }, method: :delete, data: { confirm: 'Are you sure?' } %></li></ul>
<% end %>